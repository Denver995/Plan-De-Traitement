import colors from "../../utils/colors";

const styles = {
  modal: { width: "50rem" },
  body: { marginTop: 30, marginBottom: 30 },
  message: { textAlign: "center", fontSize: 22, fontWeight: "500" },
  footer: { justifyContent: "center", padding: 0, marginBottom: 30 },
  btn: { fontSize: 20 },
};

export default styles;
