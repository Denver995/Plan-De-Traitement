import colors from "../../../utils/colors";

const styles = {
    headTitle: {
        fontSize: 30,
        color: colors.light,
        letterSpacing: 0,
        fontWeight: "bold"
    }
}

export default styles;